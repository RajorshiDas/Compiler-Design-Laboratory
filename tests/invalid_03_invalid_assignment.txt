@@ Invalid test: incompatible assignment and invalid arithmetic.
@@ Expected behavior:
@@ - cannot assign text to num
@@ - invalid arithmetic between text and num

#load mathLib.fx
start() -> empty
{
    num a;
    text msg = "hello";

    a = msg;
    a = msg + 1;
    send 0;
}
