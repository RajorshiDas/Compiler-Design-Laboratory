@@ Invalid test: use of undeclared variable in report syntax.
@@ Expected behavior: semantic errors for undeclared b.

#load mathLib.fx
start() -> empty
{
    num a = 5;
    b = a + 1;
    write(b);
    send 0;
}
