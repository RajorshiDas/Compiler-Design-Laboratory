// Invalid test: function return type used in an incompatible assignment.
// Expected behavior: semantic error assigning returned text value to num variable.

fx greet(text name) {
    return name;
}

num value;

value = greet("compiler");
