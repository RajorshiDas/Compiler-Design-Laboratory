@@ Invalid test: function declared return type does not match sent value.
@@ Expected behavior: semantic error because the function is declared num but sends text.

#load mathLib.fx
start() -> empty
{
    fx greet(text name) -> num
    {
        send name;
    }

    send 0;
}
