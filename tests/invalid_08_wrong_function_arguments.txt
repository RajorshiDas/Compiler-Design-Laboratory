@@ Invalid test: wrong number of function arguments.
@@ Expected behavior: semantic error for calling addNumbers with one argument instead of two.

#load mathLib.fx
start() -> empty
{
    num result;

    fx addNumbers(num x, num y) -> num
    {
        send x + y;
    }

    result = addNumbers(1);
    write(result);
    send 0;
}
