@@ Valid test: report-style read and multi-argument write.
@@ Sample stdin:
@@ 7
@@ Expected interpreter output with sample stdin:
@@ Read value = 7
@@ Next value = 8

#load mathLib.fx
start() -> empty
{
    num a = 1;

    read(a);
    write("Read value = ", a);
    write("Next value = ", a + 1);
    send 0;
}
