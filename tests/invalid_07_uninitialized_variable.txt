@@ Invalid test: logic context given a numeric expression.
@@ Expected behavior: semantic error because chk requires a logic expression.

#load mathLib.fx
start() -> empty
{
    num a = 1;

    chk(a + 1)
        write("bad");
    end chk

    send 0;
}
