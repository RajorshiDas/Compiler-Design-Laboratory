// Invalid test: access to a block-local variable after the block ends.
// Expected behavior: semantic error for undeclared temp outside its scope.

num a = 1;

{
    num temp = a + 1;
    write(temp);
}

write(temp);
