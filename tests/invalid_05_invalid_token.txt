@@ Invalid test: lexical errors for unsupported symbols.
@@ Expected behavior: lexical errors reported for $ and ^.

#load mathLib.fx
start() -> empty
{
    num a = 5;
    $ ^
    write(a);
    send 0;
}
