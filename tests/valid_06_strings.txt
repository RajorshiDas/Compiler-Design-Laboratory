@@ Valid test: strings and mixed write arguments.
@@ Expected interpreter output:
@@ compiler
@@ lab
@@ status = ready

#load mathLib.fx
start() -> empty
{
    text message = "compiler";
    text label = "lab";

    write(message);
    write(label);
    write("status = ", "ready");
    send 0;
}
