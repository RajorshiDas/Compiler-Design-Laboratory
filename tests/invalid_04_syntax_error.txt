@@ Invalid test: malformed repeat statement.
@@ Expected behavior: syntax error near the missing update expression.

#load mathLib.fx
start() -> empty
{
    num i = 1;
    repeat(i, i <= 3, )
    {
        write(i);
    }
    send 0;
}
