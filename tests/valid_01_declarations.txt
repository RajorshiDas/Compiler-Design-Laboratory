@@ Valid test: report-style declarations, logic literals, and character values.
@@ Expected interpreter output:
@@ 10
@@ 3.5
@@ true
@@ Z
@@ hello

#load mathLib.fx
start() -> empty;
{
    num a = 10;
    real r = 3.5;
    logic ok = true;
    chr letter = 'Z';
    text msg = "hello";

    write(a);
    write(r);
    write(ok);
    write(letter);
    write(msg);
    send 0;
}
